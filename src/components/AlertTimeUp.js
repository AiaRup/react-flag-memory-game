import React, { Component } from 'react';
import { UncontrolledAlert } from 'reactstrap';

const AlertTimeUp = () => {

const AlertTimerEnd = ()  => {
    <UncontrolledAlert color="info">
      Time Up!
    </UncontrolledAlert>
 }
  return (this.AlertTimerEnd);
}

export default AlertTimeUp;